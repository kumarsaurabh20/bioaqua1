<%= form_for(@nucleic_acid_type) do |f| %>
  <% if @nucleic_acid_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nucleic_acid_type.errors.count, "error") %> prohibited this nucleic_acid_type from being saved:</h2>

      <ul>
      <% @nucleic_acid_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
