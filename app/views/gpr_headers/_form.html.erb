<%= form_for(@gpr_header) do |f| %>
  <% if @gpr_header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gpr_header.errors.count, "error") %> prohibited this gpr_header from being saved:</h2>

      <ul>
      <% @gpr_header.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
