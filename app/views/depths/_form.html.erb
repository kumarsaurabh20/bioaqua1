<%= form_for(@depth) do |f| %>
  <% if @depth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@depth.errors.count, "error") %> prohibited this depth from being saved:</h2>

      <ul>
      <% @depth.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
