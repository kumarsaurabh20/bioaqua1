<%= form_for(@catchment_area) do |f| %>
  <% if @catchment_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@catchment_area.errors.count, "error") %> prohibited this catchment_area from being saved:</h2>

      <ul>
      <% @catchment_area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
