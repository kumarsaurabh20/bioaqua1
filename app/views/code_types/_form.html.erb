<%= form_for(@code_type) do |f| %>
  <% if @code_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_type.errors.count, "error") %> prohibited this code_type from being saved:</h2>

      <ul>
      <% @code_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
