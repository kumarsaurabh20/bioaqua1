<h1>Listing batch_images</h1>

<table>
  <tr>
  </tr>

<% @batch_images.each do |batch_image| %>
  <tr>
    <td><%= link_to 'Show', batch_image %></td>
    <td><%= link_to 'Edit', edit_batch_image_path(batch_image) %></td>
    <td><%= link_to 'Destroy', batch_image, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New batch_image', new_batch_image_path %>