<%= form_for(@filter_sample) do |f| %>
  <% if @filter_sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@filter_sample.errors.count, "error") %> prohibited this filter_sample from being saved:</h2>

      <ul>
      <% @filter_sample.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
