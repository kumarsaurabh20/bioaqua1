<%= form_for(@wfilter) do |f| %>
  <% if @wfilter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wfilter.errors.count, "error") %> prohibited this wfilter from being saved:</h2>

      <ul>
      <% @wfilter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
