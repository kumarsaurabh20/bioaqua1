<%= form_for(@batch_oligo) do |f| %>
  <% if @batch_oligo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@batch_oligo.errors.count, "error") %> prohibited this batch_oligo from being saved:</h2>

      <ul>
      <% @batch_oligo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
