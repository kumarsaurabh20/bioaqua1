<%= form_for(@microarraygal) do |f| %>
  <% if @microarraygal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microarraygal.errors.count, "error") %> prohibited this microarraygal from being saved:</h2>

      <ul>
      <% @microarraygal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
