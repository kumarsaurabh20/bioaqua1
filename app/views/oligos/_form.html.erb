<%= form_for(@oligo) do |f| %>
  <% if @oligo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oligo.errors.count, "error") %> prohibited this oligo from being saved:</h2>

      <ul>
      <% @oligo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
